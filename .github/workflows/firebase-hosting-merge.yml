# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_BUILD_SHA: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NITBOARD_57DE2 }}'
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Create .env.production for Vite build
        run: |
          BUILD_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          cat <<EOF > .env.production
          VITE_FIREBASE_API_KEY=${VITE_FIREBASE_API_KEY}
          VITE_FIREBASE_AUTH_DOMAIN=${VITE_FIREBASE_AUTH_DOMAIN}
          VITE_FIREBASE_PROJECT_ID=${VITE_FIREBASE_PROJECT_ID}
          VITE_FIREBASE_STORAGE_BUCKET=${VITE_FIREBASE_STORAGE_BUCKET}
          VITE_FIREBASE_MESSAGING_SENDER_ID=${VITE_FIREBASE_MESSAGING_SENDER_ID}
          VITE_FIREBASE_APP_ID=${VITE_FIREBASE_APP_ID}
          VITE_FIREBASE_MEASUREMENT_ID=${VITE_FIREBASE_MEASUREMENT_ID}
          VITE_BUILD_SHA=${VITE_BUILD_SHA}
          VITE_BUILD_TIME=${BUILD_TIME}
          EOF
          test -s .env.production || (echo ".env.production is empty" && exit 1)
      - name: Build
        run: npm ci && npm run build
      - name: Verify Firebase config in bundle
        run: |
          grep -R -q "nitboard-57de2" dist || (echo "Firebase project id not injected into bundle" && exit 1)
          grep -R -q "firebaseapp.com" dist || (echo "Firebase auth domain not injected into bundle" && exit 1)
      - name: Preflight Firestore deploy permissions
        run: |
          if ! npx firebase-tools firestore:indexes --project nitboard-57de2; then
            echo "Firestore preflight failed. The service account may be missing required roles (Firebase Rules Admin / Firestore Index Admin) or firebaserules.googleapis.com is disabled."
            exit 1
          fi
      - name: Deploy Firestore rules and indexes
        run: |
          # Required IAM roles for the service account:
          # - Firebase Rules Admin (or equivalent)
          # - Firestore/Datastore Index Admin (or equivalent)
          # Ensure firebaserules.googleapis.com is enabled in the project.
          npx firebase-tools deploy --only firestore:rules,firestore:indexes --project nitboard-57de2
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NITBOARD_57DE2 }}
          channelId: live
          projectId: nitboard-57de2
